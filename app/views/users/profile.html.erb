<div class="border-2 px-20">
  <div class='border-2 grid sm:grid-cols-3 md:grid-cols-3 lg:grid-cols-3 xl:grid-cols-3 mt-10 mb-20'>
    <div class='flex'>
    
      <div class='inline'>
        <img class='' src="https://picsum.photos/80/80" alt="">
      </div>
      <div>  
        <div class='inline-block'>
          <h1 class='text-lg mb-2'><%= current_user.name %></h1>
        </div>
        <div>
          <a href="#" class='text-sm'><%= sanitize "#{User.now_rank_icon(current_user.accumulated_amount)}" %>
                                    <%="#{User.now_user_rank(current_user.accumulated_amount)} 
                                    #{current_user.country}" %>
                                    </a> 
      
        </div>
      
        <div class=''>
          <a href="#"><i class="fab fa-bitcoin"></i>   <%=current_user.p_coins %></a>
        </div>
        <div class="">
          <p><i class="fas fa-star"></i>  current_user.score</p>
        </div>
      </div>
    </div>

    <div class="border">
      <div class="">
        <h2 class="text-lg mt-2 ml-3">當年度升級任務</h2>
      </div>
      <div class="">
        <% if current_user.accumulated_amount >= 20000 %>
          <p class="mt-2 ml-3">您已達成最高等級<%= sanitize User.user_rank(current_user.accumulated_amount) %></p>
        <% else %>
          <p class="mt-2 ml-3">完成以下一項條件,可立即升級為<%= sanitize User.user_rank(current_user.accumulated_amount) %></p>
        <% end %>
      </div>
      <div class="flex mt-3 ml-8">
        <div>
          <p><%= sanitize User.up_rank_icon(current_user.accumulated_amount) %></p>
        </div>
        <div>
          <% if current_user.accumulated_amount >= 20000 %>
            <p>恭喜您已達成任務</p>
          <% else %>
            <p>消費金額還需 NT$<%= User.still_need(current_user.accumulated_amount) %></p>
          <% end %>
        </div>
      </div>
    </div>
  

    <div class="border">
      <div class="">
        <h2 class="text-lg mt-2 ml-3">下一年度升級任務</h2>
      </div>
      <div>
        <% if current_user.accumulated_amount >= 20000 %>
          <p>恭喜您已達成任務</p>
        <% else %>
          <p class="mt-2 ml-3">完成以下一項條件,可在下一個年度升級為<%= sanitize User.user_rank(current_user.accumulated_amount) %></p>
        <% end %>
      </div>
      <div class="flex mt-3 ml-8">
        <div>
          <p><%= sanitize User.up_rank_icon(current_user.accumulated_amount) %></p>
        </div>
        <div>
          <% if current_user.accumulated_amount >= 20000  %>
            <p>恭喜您在下一年度為尊貴會員</p>
          <% else %>
            <p>消費金額還需 NT$<%= User.still_need(current_user.accumulated_amount) %></p>
          <% end %>
        </div>
      </div>
    </div>
  </div>

  <div class="tabs block md:grid-cols-1 lg:grid-cols-1 ">
    <a href="#" class="tab tab-bordered">關於我</a> 
  </div>
  <div class="block mt-4">
    <h2>個人名片</h2>
    <div class="flex mt-1">
      <div class="mr-19">性別</div>
      <div class=""><%= current_user.gender %></div>
    </div>
    <div class="flex mt-1">
      <div class="mr-11">現居住地</div>
      <div class=""><%= current_user.country %></div>
   </div>
    <div class="flex mt-1">
      <div class="mr-11">加入日期</div>
      <div class=""><%= current_user.created_at.strftime("%Y/%m/%d") %></div> 
    </div>
    <div class="flex mt-1">
      <div class="mr-3">個人資料設定</div>
      <div class="">
        <p><%= link_to "點我更新你的個人介紹",about_member_index_path %></p>
        <p><%= link_to "點我設定暱稱、大頭貼照、生日...", edit_member_index_path %></p>
      </div>
    </div>
    <div class="block mt-1 border-3">
      <div class="">小提醒</div>
      <div class="">在找你關注的設計館嗎？現在你可以在 慾望清單 裡看到喔。</div>
    </div>
  </div>
</div>




